
<% render 'results'%>

<%= form_for @bookings do |f| %>
<div class="row">
	<% (1..@number_of_passengers.to_i).each do |n| %>

	  <div class="large-2 columns">
	    <%= f.fields_for @passenger do |p| %>
	  </div>
	
	  	<div class="large-2 columns">
	  	  <%= p.text_field :name %>
	  	</div>
	  	
	  	<div class="large-2 columns">
	  	  <%= p.text_field :email %>
	  	</div>
	   
	   	<% end %>
	   
	   <% end %>
	 <div class="large-4 columns">
	 <%= f.submit "Search", :name => nil, class: "button" %>
  </div>
<% end %>